sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yasmany/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true yasmany/react-test npm test

after_success:
  - docker build -t yasmany/multi-client ./client
  - docker build -t yasmany/multi-nginx ./nginx
  - docker build -t yasmany/multi-server ./server
  - docker build -t yasmany/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yasmany/multi-client
  - docker push yasmany/multi-nginx
  - docker push yasmany/multi-server
  - docker push yasmany/multi-worker
